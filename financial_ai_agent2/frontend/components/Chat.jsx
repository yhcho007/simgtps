import {useState} from 'react'
export default function Chat(){const [m,setM]=useState([]);const [t,setT]='';async function s(){if(!t) return; setM(x=>[...x,{role:'user',text:t}]); const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:t})}); const j=await r.json(); setM(x=>[...x,{role:'assistant',text:j.answer}]); setT('')} return (<div style={{width:600}}><div style={{height:300,overflow:'auto',border:'1px solid #ddd',padding:10}}>{m.map((a,i)=>(<div key={i} style={{textAlign:a.role==='user'?'right':'left'}}><b>{a.role}:</b> {a.text}</div>))}</div><input value={t} onChange={e=>setT(e.target.value)} style={{width:'80%'}} onKeyDown={e=>e.key==='Enter'&&s()} /><button onClick={s}>Send</button></div>)}
